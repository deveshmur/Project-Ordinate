spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ordinate_dev
    username: ordinate
    password: ordinate
    driver-class-name: org.postgresql.Driver

  jpa:
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug