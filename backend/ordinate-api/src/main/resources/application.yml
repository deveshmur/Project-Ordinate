server:
  port: 8080

spring:
  application:
    name: ordinate-api
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate

management:
  endpoints:
    web:
      exposure:
        include: health,info